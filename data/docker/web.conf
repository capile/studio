server {
    listen       80;
    listen  [::]:80;
    server_name  studio;

    location / {
        root           /var/www/html;
        fastcgi_pass   studio:9000;
        fastcgi_param  SCRIPT_FILENAME  /var/www/app/app.php;
        include        fastcgi_params;
    }
}
